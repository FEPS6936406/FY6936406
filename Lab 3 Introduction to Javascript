<!DOCTYPE html>
<html>
<head>
  <title>Alert input text</title>
</head>
<body>
  <input type="text" id="Textinput" placeholder="Enter text">
  <button id="alertButton">Show Alert</button>

  <script>
  //Get a reference to the input field button
  var input = document.getElementByld("colorButton");
  var button = document.getElementById("alertButton");

   //Function to change the button colour
    function showAlert() {
        var input

    //Set the button's background colour to the random colour
    button.style.backgroundColor = randomColor;

    }

    // Add a click event listener to the button
    button.addEventListener("click", changeColor);
  </script>
</body>
</html>
